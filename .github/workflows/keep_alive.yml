name: Keep Streamlit App Alive

on:
  schedule:
    # 매 3일마다 자정(UTC 기준)에 실행
    - cron: '0 0 */3 * *'
  workflow_dispatch: # 수동 실행 버튼 추가

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -sL "https://imnotai.streamlit.app" > /dev/null
